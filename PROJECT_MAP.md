# PROJECT_MAP — مَوْلِدي (mawlidi.com)

موقع ثنائي اللغة لحساب تاريخ الميلاد في التقويم الهجري، مع مكتبة مقالات.
HTML/CSS/JS ثابت بلا build step وبلا npm. النشر: Cloudflare Pages (auto-deploy من فرع `main`).

## [البنية]

```
/index.html              تحويل حسب لغة المتصفح → /arb/ أو /eng/
/arb/index.html          التطبيق العربي كاملاً (1471 سطراً، CSS+JS مضمّنان)
/eng/index.html          التطبيق الإنجليزي (994 سطراً)
/arb/articles/           صفحات المقالات العربية المستقلة (14 + أرشيف)   ← مُولَّدة
/eng/articles/           صفحات المقالات الإنجليزية المستقلة (14 + أرشيف) ← مُولَّدة
/arb/converter/          محوّل التاريخ الهجري ↔ الميلادي  ← مُولَّد
/eng/converter/          نفسه بالإنجليزية                 ← مُولَّد
/arb/calendar/           التقويم الهجري السنوي القابل للطباعة ← مُولَّد
/eng/calendar/           نفسه بالإنجليزية                 ← مُولَّد
/{arb,eng}/age/          حاسبة العمر بالتقويمين           ← مُولَّدة
/{arb,eng}/countdown/    العد التنازلي للمناسبات الهجرية  ← مُولَّد
/{arb,eng}/moon/         طور القمر يوم الميلاد            ← مُولَّدة
/{arb,eng}/card/         بطاقة ميلاد قابلة للمشاركة (canvas) ← مُولَّدة
/{arb,eng}/mansions/     منازل القمر الثمانية والعشرون + تراث الأنواء ← مُولَّدة
/{arb,eng}/zodiac/       البرج التقويمي مقابل الكوكبة الحقيقية ← مُولَّدة
/assets/hijri.js         محرك التقويم الهجري الجدولي — الطبقة المشتركة
/assets/moon.js          محرك فلكي: أطوار القمر، منازله، وموضع الشمس في
                         دائرة البروج والكوكبات (Meeus مختصر) — مستقل تماماً
                         عن hijri.js: أحدهما تقويم حسابي والآخر رصد فلكي
/assets/article.css      أنماط صفحات المقالات المستقلة (مشتركة)
/assets/tool.css         أنماط صفحات الأدوات
/assets/calendar.css     أنماط التقويم السنوي (تشمل قواعد الطباعة)
/content.json            بيانات وصفية للمقالات والتصنيفات — تُحمَّل وقت التشغيل
/sitemap.xml             مُولَّد
/404.html                صفحة الخطأ — بدونها كان أي رابط خاطئ يردّ 200 (soft 404)
/robots.txt
/_headers /_redirects    إعدادات Cloudflare Pages
/tools/build.js          مولّد الصفحات و sitemap (خارج مسار النشر)
/tools/converter.js      قالب صفحة المحوّل
/tools/calendar.js       قالب صفحة التقويم السنوي
/tools/age.js            قالب حاسبة العمر
/tools/countdown.js      قالب العد التنازلي
/tools/moon.js           قالب صفحة قمر الميلاد
/tools/card.js           قالب بطاقة الميلاد
/tools/mansions.js       قالب منازل القمر (بيانات المنازل الـ28 فيه)
/tools/zodiac.js         قالب الشمس ودائرة البروج
/tools/test-hijri.js     اختبارات محرك التقويم (12)
/tools/test-moon.js      اختبارات محرك القمر (16)
```

## [مصدر الحقيقة للمقالات]

نصوص المقالات مخزّنة في مصفوفة `ARTS` **داخل** `arb/index.html:1006` و `eng/index.html:709`،
كل عنصر يحمل `body` بصيغة HTML كامل.
`content.json` يُحدّث **البيانات الوصفية فقط** (title, sub, cat, status) وقت التشغيل — لا يحمل المتن.

⚠️ بعد أي تعديل على `ARTS` يجب تشغيل:
```bash
node tools/build.js
```
وإلا تصبح الصفحات المستقلة خارج التزامن مع التطبيق.

`SLUGS` في `tools/build.js` تربط `id` المقالة برابطها الدائم.
**تغيير slug منشور يكسر الروابط ويُفقد ترتيب البحث** — أضف جديداً ولا تُعدّل قديماً.

## [لوحة التحكم]
`~/Claude Workspace/mawlidi-cms/dashboard-pro-v6.html` — خارج المستودع عمداً.
كانت منشورة على النطاق العام ويفتحها أي أحد. لا أسرار مكتوبة فيها (تطلب token
وقت التشغيل)، لكن لا مبرر لنشرها. تُفتح بالنقر المزدوج من Finder.
لإعادة نشرها بأمان: Cloudflare Zero Trust ← Access ← Applications ← تطبيق
self-hosted على `mawlidi.com/dashboard*` بسياسة تسمح لإيميلك وحده.

## [المعاينة المحلية]
```bash
python3 -m http.server 8899 --directory mawlidi   # من مجلد Claude Workspace
```
معيار النجاح: `node tools/test-hijri.js` و`node tools/test-moon.js` و
`node tools/build.js --check` تخرج كلها بحالة 0، و`xmllint --noout sitemap.xml` يمرّ.

## [ما أُنجز]
- **المرحلة 0** — robots.txt، sitemap.xml، hreflang متناظر في اللغتين
- **المرحلة 1** — تفكيك المقالات إلى 19 صفحة مستقلة + أرشيف لكل لغة
  + Article/CollectionPage schema + روابط داخلية متبادلة
  (الموقع انتقل من رابطين قابلين للفهرسة إلى 21)
- رابط `<a>` حقيقي إلى الأرشيف في تذييل الصفحتين (بقية روابط التذييل `<span onclick>` لا يراها الزاحف)
- **إصلاح خلل قديم**: `arb/index.html` كان يطلب `./content.json` (404) — النسخة العربية
  لم تقرأ `content.json` قط، فلم تظهر تعديلات لوحة التحكم عليها إطلاقاً

- **إصلاح جوهري في المحرك** — `hToJ` لم تكن عكس `jToH`، فكانت كل أعياد الميلاد
  القادمة وعدّاد الأيام خاطئة بأكثر من سنة. صارت عكساً دقيقاً ببحث ثنائي،
  والمحرك طبقة مشتركة واحدة بدل ثلاث نسخ.
- **المرحلة 2** — ثماني أدوات بلغتين: محوّل التاريخ، التقويم السنوي القابل
  للطباعة، حاسبة العمر بالتقويمين، العد التنازلي للمناسبات، طور القمر يوم
  الميلاد، بطاقة ميلاد تُرسم في المتصفح، منازل القمر الثمانية والعشرون،
  والشمس في دائرة البروج
- **المرحلة 3** — أربع مقالات جديدة موجّهة لنيّة البحث، وكل مقالة تنتهي بدعوة
  إلى الأداة التي يحتاجها قارئها (خريطة `TOOLS` في build.js)
- **تكافؤ اللغتين** — 14 مقالة في كل لغة (كانت 10 مقابل 7)
- **بوابة ظاهرة** — 5 روابط `<a>` حقيقية في كل صفحة رئيسية (بطاقة المحوّل، بطاقة
  المقالات، "عرض الكل"، ورابطا التذييل). كانت كلها `<div onclick>` لا يراها الزاحف.
- **السنة الهجرية** كانت مجمّدة على 1446 في الإحصائيات — صارت تُحسب آنياً

## [الخط التحريري في أدوات الفلك]
`mansions` و`zodiac` تعرضان حقائق فلكية محسوبة ولا تصفان شخصية ولا تتنبآن.
`zodiac` تحديداً تعرض التقسيم التقويمي **والكوكبة الحقيقية معاً** وتشرح
سبب اختلافهما (المبادرة). هذا ما يجعلها متسقة مع مقالتَي الموقع في نقد
التنجيم بدل أن تناقضهما. أي أداة قادمة في هذا الباب تلتزم القاعدة نفسها:
وصف السماء لا وصف الإنسان.

## [ORPHANS & PENDING]

| # | البند | الحالة |
|---|---|---|
| 1 | **تفعيل Cloudflare Web Analytics** | ⏳ يحتاج لوحة تحكم Cloudflare — لا قياس إطلاقاً حالياً |
| 2 | **تسجيل الموقع في Google Search Console + رفع sitemap** | ⏳ يحتاج حساب المالك |
| 4 | تكافؤ اللغتين في المقالات | ✅ 14 مقالة في كل لغة، و hreflang صار مبنياً على الوجود الفعلي لا على افتراض ثابت |
| 5 | لوحة التحكم | ✅ نُقلت خارج المستودع إلى `~/Claude Workspace/mawlidi-cms/` — لم تعد منشورة. تُفتح محلياً، وتاريخها محفوظ في git |
| 7 | `content.json` لا يحمل متن المقالات | قيد معماري — اللوحة لا تستطيع تحرير المتن |
| 8 | المرحلة 2: بقية الأدوات | ⏳ أُنجزت 8. المتبقي المقترح: الحيوان الصيني (يحتاج نقاشاً تحريرياً)، يوم الأسبوع الذي وُلدت فيه |
| 10 | **تواريخ الأنواء** | ⛔ مُستبعَد عمداً: تحديدها يقتضي ترجيحاً في خلاف قديم (السقوط أم الطلوع). الصفحة تعرض موضع الشمس المحسوب وتشرح الخلاف بدل ادّعاء تواريخ |
| 9 | المرحلة 3: مقالات جديدة موجّهة لنيّة البحث | لم تبدأ |
